module App.Model exposing (..)

import App.Types exposing (Model, Msg)

init : ( Model, Cmd Msg )
init = {
  search = ""
  --flats = [],
  --selectedFlat =
  --emptyFlat, map = mapInit }
  } ! []
  -- [ fetchFlats, mapInit |> toJsObject |> initializeMap ])
